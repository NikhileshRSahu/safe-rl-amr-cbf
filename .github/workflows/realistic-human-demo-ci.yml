name: Realistic Human Demo CI

on:
  push:
    branches: [research/beast-baselines-v1]
    paths:
      - "tests/test_realistic_human_demo.py"
      - "tests/test_human_sweep_experiment.py"
      - "tests/test_progressive_velocity_baselines.py"
      - "tests/test_progressive_ladder_protocol.py"
      - "benchmark/render_shared_success_case.py"
      - "benchmark/human_sweep_experiment.py"
      - "benchmark/progressive_velocity_baselines.py"
      - "benchmark/evaluate_progressive_ladder.py"
      - ".github/workflows/realistic-human-demo-ci.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "numpy<2.0" scipy cvxpy osqp matplotlib pytest
          pip install torch --index-url https://download.pytorch.org/whl/cpu
      - name: Run human and progressive-baseline regressions
        run: pytest -q tests/test_realistic_human_demo.py tests/test_human_sweep_experiment.py tests/test_progressive_velocity_baselines.py tests/test_progressive_ladder_protocol.py
